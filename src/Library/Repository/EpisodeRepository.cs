using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using System.Threading.Tasks;
using TilosPlayer.Library.Domain;

namespace TilosPlayer.Library.Repository
{
    public class EpisodeRepository
    {
        //private readonly string _repositoryPath;

        //public EpisodeRepository(string repositoryPath)
        //{
        //    if (string.IsNullOrWhiteSpace(repositoryPath))
        //    {
        //        throw new ArgumentException("Path cannot be null or empty.", nameof(repositoryPath));
        //    }

        //    _repositoryPath = repositoryPath;
        //}


        ////@"c:\Users\geri\source\repos\TilosPlayer\localRepository\"
        //public async Task<Episode[]> GetEpisodesByTimeRange(DateTimeOffset startDate, DateTimeOffset endDate)
        //{
        //    var start = startDate.ToUnixTimeMilliseconds();
        //    var end = endDate.ToUnixTimeMilliseconds();

        //    Directory.GetFiles("", "", SearchOption.)

        //    //var result = Newtonsoft.Json.JsonConvert.DeserializeObject<Episode[]>(responseContent);
        //    //return result;
        //}

    }
}
